import { dataRender} from '../src/client/js/dataRender';

describe("Testing data render function", () => {
  test("It should update the UI of webpage", () => {
    expect(dataRender).toBeDefined()
  } )
})